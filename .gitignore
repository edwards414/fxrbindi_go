# secrets — NEVER commit
.secrets/
*.env
untitled.txt

# large training artifacts (checkpoints ~31MB each, >2GB total)
runs/**/*.pkl
baselines/*.ckpt

# local CPU inference env (app engine server)
.venv/

# runtime state written by the engine server (persisted games)
runs/**/app_games.json

# caches / notebooks
.jax_cache/
.ipynb_checkpoints/
__pycache__/
*.pyc

# downloaded external engines (KataGo binary + nets)
engines/
*.p8
