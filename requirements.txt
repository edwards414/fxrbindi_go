# gozero runtime deps — pinned 2026-07-06 after a container rebuild (CPU/RAM
# upgrade) wiped the pip environment. Restore with:
#   pip install -r requirements.txt
# jax[cuda12] pulls its own bundled CUDA libs; host needs an NVIDIA driver
# supporting CUDA 12 (this box: driver 570.133.20 / CUDA 12.8, 8x H100).
jax[cuda12]==0.6.2
jaxlib==0.6.2
flax==0.10.7
optax==0.2.8
mctx==0.0.5
pgx==2.6.0
numpy==2.2.6
